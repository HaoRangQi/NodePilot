# Requirements

- Make the custom chrome, navigation rail, and content shell share one structural background color.
- Remove the shell-level gradients and semi-transparent background mixes that make the regions look separated.
- Keep cards as the only elevated/content surfaces.
