# Requirements

- Theme must default to following the operating system preference.
- The default `system` theme should not be persisted as an override.
- Explicit user selections for `light` or `dark` should still persist.
